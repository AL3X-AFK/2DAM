CREATE TYPE tabla_hijos IS TABLE OF VARCHAR(30);
/

CREATE TABLE empleado2(
Idemp number,
Nombre VARCHAR2(30),
Apellido VARCHAR2(30),
Hijos tabla_hijos)
NESTED TABLE Hijos STORE AS t_hijos;
/

select object_name, object_type, status
from dba_objects where objects_name like '%HIJO%';


select segment_name, segment_type from dba_segments where segments_name like '%HIJO%';

insert into empleado2 values(
	1, 'Fernando', 'Moreno', tabla_hijos('Elena','Pablo')
);
/

insert into empleado2 values(
	2, 'David', 'Sanchez', tabla_hijos('Carmen','Candela')
);
/


SELECT Emp.* from empleado2 e, TABLE (e.hijos) emp where e.Idemp = 1;

UPDATE empleado2 SET
Hijos = tabla_hijos('Carmen', 'Candela', 'Cayetana')
where Idemp = 1;

Select emp.* from empleado2 e, TABLE(e.hijos) emp;

select emp.* from empleado2 e, Table(e.hijos) emp;